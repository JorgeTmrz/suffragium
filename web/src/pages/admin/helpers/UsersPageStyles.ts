import { makeStyles } from "@material-ui/core";

export const AddOrEditUserModalStyles = makeStyles((theme) => ({
    adormentSpan: {
        whiteSpace: "nowrap",
        marginLeft: "2vw"
    }
}));