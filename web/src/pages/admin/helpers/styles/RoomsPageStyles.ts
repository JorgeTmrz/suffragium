import { makeStyles } from "@material-ui/core";

export const RoomsPageStyles = makeStyles({
    divider: {
        marginBottom: "20px",
        marginTop: "20px",
    },
    endedRoomsTitle: {
        marginBottom: "20px",
    }
});